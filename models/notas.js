const mongoose = require('mongoose');
const Schema = mongoose.Schema;

//Criando Schema
console NotaSchema = new Schema({
    
});

const Notas = mongoose.model('notas', NotaSchema);

module.exports = Notas;